# @careerhub/utils

Shared, framework-agnostic utilities for CareerHub UK.

**Scaffold only for this milestone.** UI-specific helpers (such as the Tailwind
`cn` merge helper) currently live inside `apps/web/lib/utils.ts`.
