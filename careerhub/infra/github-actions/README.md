# GitHub Actions (`infra/github-actions`)

Reusable CI/CD workflow definitions for CareerHub UK.

**Scaffold only for this milestone.** A starter `web-ci.yml` is provided as a
reference for linting and building the web app; copy it into `.github/workflows/`
to activate it. No pipelines run automatically yet.
