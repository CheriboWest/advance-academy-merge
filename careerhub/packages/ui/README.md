# @careerhub/ui

Shared UI primitives for CareerHub UK.

**Scaffold only for this milestone.** The student portal (`apps/web`) currently
owns its shadcn/ui components under `apps/web/components/ui`. Components that
prove reusable across the student portal and the coach workspace will be
promoted here later.
