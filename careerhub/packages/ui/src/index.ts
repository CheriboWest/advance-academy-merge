/**
 * Shared UI primitives for CareerHub UK.
 *
 * Scaffold for this milestone. The student portal (`apps/web`) currently owns
 * its own shadcn/ui components under `apps/web/components/ui`. Components that
 * prove reusable across the student portal and the coach workspace will be
 * promoted into this package in a later milestone.
 */

export {};
