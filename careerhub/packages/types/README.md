# @careerhub/types

Shared TypeScript types for CareerHub UK.

**Scaffold only for this milestone.** The public student portal (`apps/web`)
currently keeps local copies of these types next to its mock data so it can run
standalone. Once the FastAPI backend and coach workspace land, this package
becomes the single source of truth consumed by every app.
