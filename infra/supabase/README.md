# Supabase (`infra/supabase`)

Supabase project configuration, migrations, and seed data for CareerHub UK.

**Scaffold only for this milestone.** Supabase is not connected yet — no
project, schema, migrations, or seeds are defined. This folder reserves the
location for them.
