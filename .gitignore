# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables (secrets — do not commit)
.env
.env.*
!.env*.example

# Common ignores
node_modules
.next/
.DS_Store
.claude/
backend/dist/
# build artifacts — conflict on every merge, regenerated by tsc
*.tsbuildinfo

# backend-python (career-hub's FastAPI service). Its ignores used to live in
# career-hub's repo root, which the subtree move left behind.
.venv/
__pycache__/
*.pyc
.pytest_cache/
