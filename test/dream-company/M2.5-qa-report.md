# M2.5 — Dream Company QA & handover readiness

Date: 2026-07-03 · Owner: Giang · Branch under test: `feat/dream-company-m2-speed`
(M1 merged to main + live on prod; M2.1/M2.2/M2.4 verified here pre-merge.)

**Environment note:** per the owner's direction, Supabase data access is **simulated** — QA runs
against a local backend using the dev Supabase project (`tiumnkknbgnqhrvohnxy`) with
throwaway "student" accounts (created + deleted per run). The prod webapp login
(`conor.advance@gmail.com`) is for a human doing the final visual round on the deployed app,
not for programmatic QA. Prod itself was verified token-free earlier (routing/auth/no-404).

## Sample students — end-to-end, no errors

**Student #1 — JSON path (full AC matrix, `m1-ac-check.mjs`):** 6/6 passed.

| Check | Result |
|---|---|
| Unauthed → 401 (not 404/500) | ✅ |
| analyze → 200 valid | ✅ 29.0s · marketLevel=mid |
| roles → 200 valid array | ✅ 27.2s · **10 roles** (M2.2) |
| roadmap → 200, 3 phases + 20 jobs | ✅ 60.7s · jobsError=null |
| parse-cv valid DOCX → 200 parsed | ✅ 3.8s |
| parse-cv unsupported .txt → 400 | ✅ |

**Student #2 — streaming path (SSE, M2.1):** all 3 endpoints OK.

| Stream endpoint | Result |
|---|---|
| analyze/stream | ✅ 58 deltas, 25.3s, valid `done` |
| roles/stream | ✅ 10 roles, 54 deltas, 23.3s |
| roadmap/stream | ✅ 3 phases + 20 jobs, 136 deltas, 56.7s |

## Edge cases / robustness (verified across M1–M2.1)

| Case | Result |
|---|---|
| No token | 401 UNAUTHORIZED |
| SDK timeout | JSON 504 / SSE `error{code:TIMEOUT}`, aborts <1s, no hang |
| Truncated output (max_tokens) | JSON 502 / SSE `error{code:LLM_TRUNCATED}`, per-step log |
| Corrupt/oversized CV | 422 friendly, no 500 |
| Live jobs unavailable | `jobsError` surfaced in amber box on UI (`dream-company-screen.tsx`) |
| Mid-stream disconnect | reader rejects → spinner stops (FE), heartbeat keeps live conn open |

## Onboarding checklist

- [x] A fresh (never-seen) student account can complete the whole flow with no errors.
- [x] Two sample students exercised (JSON + streaming) — both smooth.
- [x] No 404/500 on any endpoint; auth gate enforced.
- [x] Speed: full 3-step ≈ 25+23+57 ≈ 105s of model time, with live progress the whole way.
- [ ] **Final human round (owner):** log into the deployed webapp and eyeball the UI
      (progress bar, streaming reveal, results) — this needs a browser (not automatable here).

## Follow-ups / cleanup

- ⚠️ One **unconfirmed** prod user was left by an earlier signup probe:
  `qa-student-1783072881903@advanceacademy-qa.com` (userId `4e5bec52-…`). It's inert
  (unconfirmed, no session) but couldn't be deleted without the prod `service_role` key.
  Delete it from Supabase dashboard (project `veiwvrhamecmabcxnltj` → Authentication → Users),
  or raise the Railway agent limit and I'll remove it.
- M2.5 AC "log into Vi's account + final round" → owner does this on the deployed webapp
  after M2 merges (the automatable QA above is green).
